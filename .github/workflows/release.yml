---
name: Release

on:
  workflow_run:
    types:
      - completed
    workflows:
      - "ci"
    branches:
      - release/*
jobs:
  get-tag:
    name: Get tag
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    outputs:
      tag: ${{ steps.get-tag-step.outputs.tag }}
    steps:
      - name: Get tag
        run: |
          branch=${{github.event.workflow_run.head_branch}}
          echo tag=${branch#release/} >> $GITHUB_OUTPUT
        id: get-tag-step

  create-release:
    needs: [get-tag, check-gomod-deps]
    uses: networkservicemesh/.github/.github/workflows/release.yaml@main
    secrets:
      token: ${{ secrets.NSM_BOT_GITHUB_TOKEN }}

  release-dependent-repositories:
    name: Update cmd-backend-ui repository
    runs-on: ubuntu-latest
    needs: get-tag
    steps:
      - name: Checkout networkservicemesh/cmd-backend-ui repository
        uses: actions/checkout@v4
        with:
          path: bszirtes/cmd-backend-ui
          repository: bszirtes/cmd-backend-ui
          token: ${{ secrets.NSM_BOT_GITHUB_TOKEN }}

      - name: Push branch to the cmd-backend-ui
        working-directory: bszirtes/cmd-backend-ui
        run: |
          echo Starting to release repository cmd-backend-ui
          git checkout -b ${{ github.event.workflow_run.head_branch }}
          git push -f origin ${{ github.event.workflow_run.head_branch }}
